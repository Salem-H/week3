let identifier = "value";

let foodCost = 79.25;
let tax = 6.54;
let tip = 12.00;

let totalDue = foodCost + tax + tip;

console.log(`
    Food cost is $${foodCost} and tax is $${tax}.
    Tip is $${tip}.
    The total due is $${totalDue}.
`);

console.log("Tip is " + tip.toFixed(2))
